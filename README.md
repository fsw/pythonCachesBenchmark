pythonCachesBenchmark
=====================

My first steps in python.
Might contain a lot of gibberish and be not 'pythonic'.

i run it with
sudo /etc/init.d/memcached restart && rm catcache.p* && python test.py

